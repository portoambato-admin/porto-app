import 'package:flutter/material.dart';

class RepresentanteMensualidadDetalleScreen extends StatelessWidget {
  const RepresentanteMensualidadDetalleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Detalle mensualidad')),
      body: const Center(child: Text('TODO: detalle + estado + comprobante si aplica')),
    );
  }
}
